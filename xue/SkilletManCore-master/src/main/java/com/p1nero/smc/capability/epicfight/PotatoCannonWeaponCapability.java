package com.p1nero.smc.capability.epicfight;

import yesman.epicfight.world.capabilities.item.CapabilityItem;
import yesman.epicfight.world.capabilities.item.RangedWeaponCapability;

public class PotatoCannonWeaponCapability extends RangedWeaponCapability {
    public PotatoCannonWeaponCapability(CapabilityItem.Builder builder) {
        super(builder);
    }

    @Override
    public ZoomInType getZoomInType() {
        return super.getZoomInType();
    }
}
